// is doesnt stop when changes window or tab
//so after u reopen it it runs all the time wou were away, at once
//maybe should stop when you are not viewing this tab

if (accumulatedTime > 10*deltaTime){ 
    accumulatedTime = 10*deltaTime
    }